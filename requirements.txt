# Core dependencies
# Note: ai-edge-litert is preferred for TFLite inference
ai-edge-litert>=2.0.0
pyserial>=3.5
bleak>=0.22.0
onnxruntime>=1.16.0  # For ONNX models (YOLO, etc.)
tensorflow>=2.21.0  # Fallback if ai-edge-litert unavailable
opencv-python-headless>=5.0.0
flask>=3.0.0
pyyaml>=6.0
numpy>=2.0.0

# Audio processing
sounddevice>=0.5.0
soundfile>=0.14.0

# Optional (for development)
# pytest>=8.0.0